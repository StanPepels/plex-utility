<UserControl x:Class="Plex_Util.FilterBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Plex_Util"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
  <UserControl.Resources>
    <local:ContentToBoolConverter x:Key="ContentToBoolConverter" />
  </UserControl.Resources>
  <ItemsControl x:Name="items" VerticalAlignment="Stretch" Margin="5" >
    <ItemsControl.ItemsPanel>
      <ItemsPanelTemplate>
        <StackPanel Orientation="Horizontal"/>
      </ItemsPanelTemplate>
    </ItemsControl.ItemsPanel>
    <ItemsControl.ItemTemplate>
      <DataTemplate>
        <ToggleButton Margin="5,0" Padding="5,0"  VerticalAlignment="Center" Click="HandleButtonClickedEvent" Content="{Binding Name}" >
          <ToggleButton.IsChecked>
            <MultiBinding Converter="{StaticResource ContentToBoolConverter}" Mode="OneWay">
              <Binding Path="SelectedButton" RelativeSource="{RelativeSource AncestorType=local:FilterBar}" />
              <Binding Path="Content" RelativeSource="{RelativeSource Self}" />
            </MultiBinding>
          </ToggleButton.IsChecked>
        </ToggleButton>
      </DataTemplate>
    </ItemsControl.ItemTemplate>
  </ItemsControl>
</UserControl>
